from starlette.routing import Route, Mount

# from vocata.graph.authz import AccessMode, PUBLIC_ACTOR

routes = [
    # Route("/", endpoint=AdminDashboardEndpoint, name="dashboard")
    # Mount("/", routes=signin.routes, name="auth"),
]
